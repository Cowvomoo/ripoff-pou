{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mongoose": "^6.9.2",
    "nodemon": "^2.0.20"
  }
}
